<Window x:Class="CarCare360.Desktop.Views.PartEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource LightBackgroundBrush}">

    <Border Padding="28,24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding Title}"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource PrimaryDarkBlueBrush}"
                       Margin="0,0,0,20" />

            <!-- Название -->
            <StackPanel Grid.Row="1" Margin="0,0,0,14">
                <TextBlock Text="Название *" FontSize="12" Foreground="#555555" Margin="0,0,0,4" />
                <TextBox Style="{StaticResource ThemedTextBoxStyle}"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <!-- Артикул -->
            <StackPanel Grid.Row="2" Margin="0,0,0,14">
                <TextBlock Text="Артикул" FontSize="12" Foreground="#555555" Margin="0,0,0,4" />
                <TextBox Style="{StaticResource ThemedTextBoxStyle}"
                         Text="{Binding PartNumber, UpdateSourceTrigger=PropertyChanged}"
                         FontFamily="Consolas" />
            </StackPanel>

            <!-- Остаток + Цена -->
            <Grid Grid.Row="3" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Остаток (шт.)" FontSize="12" Foreground="#555555" Margin="0,0,0,4" />
                    <TextBox Style="{StaticResource ThemedTextBoxStyle}"
                             Text="{Binding StockText, UpdateSourceTrigger=PropertyChanged}"
                             HorizontalContentAlignment="Right" />
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Цена, ₽" FontSize="12" Foreground="#555555" Margin="0,0,0,4" />
                    <TextBox Style="{StaticResource ThemedTextBoxStyle}"
                             Text="{Binding PriceText, UpdateSourceTrigger=PropertyChanged}"
                             HorizontalContentAlignment="Right" />
                </StackPanel>
            </Grid>

            <!-- Ошибка -->
            <Border Grid.Row="4" Background="#FFEBEE" CornerRadius="4"
                    Padding="12,8" Margin="0,0,0,16">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource ErrorRedBrush}"
                           FontSize="13" TextWrapping="Wrap" />
            </Border>

            <!-- Кнопки -->
            <Grid Grid.Row="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Отмена" Command="{Binding CancelCommand}" Height="40" FontSize="14">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="White" />
                            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBlueBrush}" />
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBlueBrush}" />
                            <Setter Property="BorderThickness" Value="1.5" />
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#E8EAF6" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="2" Content="  Сохранить  "
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Height="40" FontSize="14" />
            </Grid>
        </Grid>
    </Border>
</Window>

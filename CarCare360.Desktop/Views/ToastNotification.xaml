<UserControl x:Class="CarCare360.Desktop.Views.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="300" Margin="0,0,0,8">

    <Border x:Name="ToastBorder" CornerRadius="8" Padding="14,12">
        <!-- Начальное положение: за правым краем экрана -->
        <Border.RenderTransform>
            <TranslateTransform X="320"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" Opacity="0.25" ShadowDepth="3"/>
        </Border.Effect>

        <!-- Анимация выезда при появлении -->
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="#2E7D32"/>
                <Style.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                    To="0" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="IconText"
                       Grid.Column="0"
                       Text="✔"
                       FontSize="17"
                       Foreground="White"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>

            <TextBlock x:Name="MessageText"
                       Grid.Column="1"
                       Foreground="White"
                       FontSize="13"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>

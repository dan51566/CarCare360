<UserControl x:Class="CarCare360.Desktop.Views.ClientCarsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:CarCare360.Desktop.Views"
             Background="{StaticResource LightBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="White"
                BorderBrush="{StaticResource BorderGrayBrush}"
                BorderThickness="0,0,0,1" Padding="24,16">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xE804;" FontFamily="Segoe MDL2 Assets"
                               FontSize="24" VerticalAlignment="Center"
                               Foreground="{StaticResource PrimaryDarkBlueBrush}"
                               Margin="0,0,12,0"/>
                    <TextBlock Text="Мои автомобили" FontSize="22" FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryDarkBlueBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Command="{Binding RefreshCommand}"
                            Style="{StaticResource NeutralButtonStyle}"
                            Width="38" Padding="0" ToolTip="Обновить" Margin="0,0,10,0">
                        <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                    </Button>
                    <Button Command="{Binding AddCarCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            ToolTip="Добавить новый автомобиль">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets"
                                       FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Добавить авто" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Skeleton / DataGrid -->
        <views:SkeletonLoading Grid.Row="1" Margin="16,8,16,0"
                               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Cars}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" AlternatingRowBackground="#FAFAFA"
                  RowBackground="White" BorderThickness="0"
                  CanUserResizeRows="False" FontSize="13" Margin="16,8,16,0"
                  Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibility}}">
            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3F51B5"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBlueBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="12,10"/>
                    <Setter Property="Height" Value="38"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Гос. номер"   Binding="{Binding LicensePlate}" Width="130" />
                <DataGridTextColumn Header="Марка / Модель" Binding="{Binding BrandModel}" Width="*" />
                <DataGridTextColumn Header="Год"          Binding="{Binding Year}"          Width="70" />
                <DataGridTextColumn Header="VIN"          Binding="{Binding VIN}"           Width="170"/>
                <DataGridTextColumn Header="Цвет"         Binding="{Binding Color}"         Width="110"/>
                <DataGridTextColumn Header="Пробег (км)"  Binding="{Binding Mileage, StringFormat={}{0:N0}}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Статусбар -->
        <Border Grid.Row="2" Background="White"
                BorderBrush="{StaticResource BorderGrayBrush}"
                BorderThickness="0,1,0,0" Padding="24,8">
            <TextBlock Text="{Binding Status}" Foreground="#666" FontSize="12"/>
        </Border>
    </Grid>
</UserControl>

<Application x:Class="CarCare360.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CarCare360.Desktop"
             xmlns:helpers="clr-namespace:CarCare360.Desktop.Helpers"
             Startup="App_OnStartup">
    <Application.Resources>

        <!-- ===== Конвертеры ===== -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <helpers:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility" />
        <helpers:AuditOperationTextConverter  x:Key="AuditOpText" />
        <helpers:AuditOperationColorConverter x:Key="AuditOpColor" />
        <helpers:QuantityToLowStockConverter  x:Key="QuantityToLowStock" />

        <!-- ===== Цветовая палитра CarCare 360 ===== -->
        <Color x:Key="PrimaryDarkBlueColor">#1A237E</Color>
        <Color x:Key="PrimaryDarkBlueHoverColor">#283593</Color>
        <Color x:Key="AccentOrangeColor">#FF6B00</Color>
        <Color x:Key="AccentOrangeHoverColor">#E65C00</Color>
        <Color x:Key="LightBackgroundColor">#F5F5F5</Color>
        <Color x:Key="BorderGrayColor">#D0D0D0</Color>
        <Color x:Key="ErrorRedColor">#D32F2F</Color>

        <SolidColorBrush x:Key="PrimaryDarkBlueBrush"      Color="{StaticResource PrimaryDarkBlueColor}" />
        <SolidColorBrush x:Key="PrimaryDarkBlueHoverBrush" Color="{StaticResource PrimaryDarkBlueHoverColor}" />
        <SolidColorBrush x:Key="AccentOrangeBrush"         Color="{StaticResource AccentOrangeColor}" />
        <SolidColorBrush x:Key="AccentOrangeHoverBrush"    Color="{StaticResource AccentOrangeHoverColor}" />
        <SolidColorBrush x:Key="LightBackgroundBrush"      Color="{StaticResource LightBackgroundColor}" />
        <SolidColorBrush x:Key="BorderGrayBrush"           Color="{StaticResource BorderGrayColor}" />
        <SolidColorBrush x:Key="ErrorRedBrush"             Color="{StaticResource ErrorRedColor}" />

        <!-- ===== Базовый стиль TextBox ===== -->
        <Style x:Key="ThemedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding"                  Value="10,8" />
            <Setter Property="FontSize"                 Value="14" />
            <Setter Property="BorderThickness"          Value="1" />
            <Setter Property="BorderBrush"              Value="{StaticResource BorderGrayBrush}" />
            <Setter Property="Background"               Value="White" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <!-- ===== Базовый стиль PasswordBox ===== -->
        <Style x:Key="ThemedPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Padding"                  Value="10,8" />
            <Setter Property="FontSize"                 Value="14" />
            <Setter Property="BorderThickness"          Value="1" />
            <Setter Property="BorderBrush"              Value="{StaticResource BorderGrayBrush}" />
            <Setter Property="Background"               Value="White" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <!-- ===== Основная (акцентная) оранжевая кнопка ===== -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background"   Value="{StaticResource AccentOrangeBrush}" />
            <Setter Property="Foreground"   Value="White" />
            <Setter Property="FontSize"     Value="14" />
            <Setter Property="FontWeight"   Value="SemiBold" />
            <Setter Property="Padding"      Value="16,10" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor"       Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{StaticResource AccentOrangeHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== Нейтральная серая кнопка (вторичные действия) ===== -->
        <Style x:Key="NeutralButtonStyle" TargetType="Button">
            <Setter Property="Background"   Value="#E0E0E0" />
            <Setter Property="Foreground"   Value="#424242" />
            <Setter Property="FontSize"     Value="14" />
            <Setter Property="FontWeight"   Value="SemiBold" />
            <Setter Property="Padding"      Value="16,10" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor"       Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#BDBDBD" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== Анимированный стиль строки DataGrid (hover 200ms) ===== -->
        <!-- Важно: DataGrid должен иметь RowBackground="White", иначе анимация ColorAnimation не сработает -->
        <Style x:Key="AnimatedDataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Height"  Value="44"/>
            <Setter Property="Cursor"  Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
                                    Storyboard.TargetProperty="(DataGridRow.Background).(SolidColorBrush.Color)"
                                    To="#F0F2FF" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
                                    Storyboard.TargetProperty="(DataGridRow.Background).(SolidColorBrush.Color)"
                                    To="White" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>

                <!-- Баг 1 — выделенная строка: явно задаём фирменный синий фон и белый текст,
                     чтобы текст был читаем при любом выделении (без этого может наследоваться
                     тёмный Foreground, невидимый на синем фоне). -->
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1A237E"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>

                <!-- Выделенная строка + hover: чуть светлее, чтобы подтвердить наведение. -->
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected"   Value="True"/>
                        <Condition Property="IsMouseOver"  Value="True"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="#283593"/>
                    <Setter Property="Foreground" Value="White"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>

        <!-- ===== Маленькая иконка-кнопка (строчные действия в DataGrid) ===== -->
        <Style x:Key="SmallIconButtonStyle" TargetType="Button">
            <Setter Property="Width"        Value="28" />
            <Setter Property="Height"       Value="28" />
            <Setter Property="Background"   Value="#E0E0E0" />
            <Setter Property="Foreground"   Value="#424242" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor"       Value="Hand" />
            <Setter Property="Padding"      Value="0" />
            <Setter Property="FontSize"     Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#BDBDBD" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="b" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
